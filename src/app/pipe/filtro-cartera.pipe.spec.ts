import { FiltroCarteraPipe } from './filtro-cartera.pipe';

describe('FiltroCarteraPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCarteraPipe();
    expect(pipe).toBeTruthy();
  });
});
