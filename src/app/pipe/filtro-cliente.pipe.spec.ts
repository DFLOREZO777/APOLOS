import { FiltroClientePipe } from './filtro-cliente.pipe';

describe('FiltroClientePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroClientePipe();
    expect(pipe).toBeTruthy();
  });
});
